'use babel';

import ReactThreePackageView from '../lib/react-three-package-view';

describe('ReactThreePackageView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
